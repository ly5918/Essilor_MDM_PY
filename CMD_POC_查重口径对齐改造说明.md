# 查重（Duplicate Check）口径对齐改造说明

依据：《Essilor CMD POC 总体设计方案 V6.1》

- 场景一泳道节点 Duplicate Check：**「以信用代码和经营地址为主依据；名称仅作为辅助线索」**
- 匹配规则页：**「配置信用代码、经营地址、辅助名称、字段组合和空值策略」** +
  **「定义 Exact / Suspected / New 阈值与 Same-BU / Cross-BU 路由」**
- 输出候选与解释：**「展示字段贡献、相似度、候选 One ID 和结果分类」**

---

## 一、结论：在途申请触发疑似匹配是**对的**，不需要去掉

- 文档虽未在创建场景明写「在途申请」，但场景二明确要求 **「批次内去重并与 CMD 存量匹配」**——
  尚未入库的数据之间也要查重；创建场景的对等实现就是查在途申请。
- 业务必要性：不判的话，同一主体的两条申请各自审批通过会产生两个 One ID，
  这正是 MDM 要根治的问题。
- 因此「还没入库就触发疑似匹配」符合设计意图，予以保留。

## 二、改造项

### 1. 匹配依据全面对齐文档（核心）

`backend/app/services/duplicate.py` 重写为**规则驱动**：

| 项 | 改造前 | 改造后 |
|---|---|---|
| 主依据 | 仅信用代码 | 统一社会信用代码 + 经营地址 |
| 客户名称 | 单独 Dice≥0.85 即可判 SUSPECTED | 降级为辅助线索，主依据不足时不单独判重复 |
| 阈值 | 硬编码 0.85，改规则页无效 | 读取 `match_rule`（CREATE: 95/70；IMPORT: 92/65） |
| 标准化 | 写死的后缀表 | 解析 `match_rule.normalize_rule`：trim / upper / removeSuffix / fullToHalf |
| 输出 | 只有 match_state | 增加得分、字段贡献（信用代码/经营地址/客户名称各多少分）、判定依据 |

打分公式：`0.75 × max(信用代码相似, 经营地址相似) + 0.25 × 名称相似`
- 主依据相似度 < 50 时，总分被压到疑似线以下 → 名称再像也不判重复；
- 双方主依据都无值可比时，名称按 100% 权重兜底（空值策略）。

**硬约束（新增）**：双方信用代码都有值却不相等 → 最高只判 SUSPECTED。
避免「同址同名的总公司 / 分公司」被自动合并成同一个 One ID。

实测口径（`_test_duplicate.py`）：

| 场景 | 得分 | 判定 |
|---|---:|---|
| 信用代码相同 | 100 | EXACT |
| 代码不同 + 地址相同 + 名称近似（总/分公司） | 95.6 | SUSPECTED（代码冲突降级，不自动合并） |
| 代码不同 + 地址相近 + 名称相近 | 81.3 | SUSPECTED |
| 仅名称相同（代码/地址均不同） | 36.5 | NEW（名称仅辅助） |
| 完全不同 | 3.1 | NEW |

### 2. 审批证据文案纠错（P1）

`customer.py` 原先写死「命中存量主档」，在途命中时也说命中主档——
审批人 / Auditor 会看到一条库里并不存在的记录。现按命中来源区分：

- 已发布主档 → 「主依据（信用代码 / 经营地址）命中已发布主档」
- 在途申请 → 「同一统一社会信用代码存在尚未审批完成的在途申请（申请编号…），库中暂无该主体的已发布主档」

证据新增字段：`命中来源`、`在途申请`、`匹配得分`、`字段贡献`、`匹配口径`。

### 3. 在途重复的处置动作（P2）

`approval.py` 新增 `_is_in_flight_duplicate()`：命中的是在途申请时，
不再渲染「确认合并 / 确认关联已有」（对方还不是主档，合并语义不成立），改为：

- 「确认重复·撤回本单」
- 「确认为不同主体·继续新建」
- 「退回补充 / 退回BU修正信用代码」

判断标签同步改为「重复发单：同一信用代码存在尚未审批完成的在途申请…」。

### 4. 同码双主档防线（P0，真实风险）

`cmd_customer.credit_code` 只是普通索引（`idx_cmd_customer_credit`，非唯一），
且发布时只做 one_id 幂等守卫、不复查同码主档 → 两条同码申请都批准就会产生
两条同信用代码的 active 黄金记录。

`publish_customer()` 新增发布前复查：

- 同信用代码已有 active 主档 → **绝不新建**，本单关联到该主档（即使审批动作是
  「排除重复·继续新建」，也会强制关联并在 remark 记录原因）；
- 治理者选择「关联 / 合并」 → 按重新查重的结果链路到最佳候选主档；
- 本条后提交却先被批准、同码还有更早的在途申请 → 在备注留痕提示跟进前一单。

事务内验证（`_test_publish_guard.py`，已回滚未落库）：

```
发布前：同信用代码 active 主档 = 1 条
待发布：AP-20260923-0001  one_id=GC-00000002  match_state=NEW（提交时快照）
publish_customer 返回 = GC-00000004
发布后：同信用代码主档 = 1 条（GC-00000004）
申请状态 = approved  merged_to_one_id = GC-00000004
✅ 未产生第二条同码主档
```

### 5. 批量导入同步对齐

`import_service.py`：

- 新增地址字段参与匹配（模板 `TPL_DOOR_*` 已映射且必填 `Address`）；
- 批次内去重：信用代码相同 → SUSPECTED；**名称 + 经营地址**均相同 → SUSPECTED；
  仅名称相同 → 不判重复，只在 error_summary 提示「名称仅作辅助线索」；
- 存量匹配改为同一套打分逻辑（阈值取 `MR_IMPORT_V1`：92 / 65），并保留代码冲突硬约束；
- `import_repo.list_active_for_match()` 提供候选集（one_id / 名称 / 信用代码 / 地址）。

### 6. 查重接口扩展

`GET /cmd/duplication/candidate` 新增可选参数 `legalName`、`address`，
与主依据口径一致（原仅有 creditCode）。

---

## 三、改动文件

| 文件 | 说明 |
|---|---|
| `backend/app/services/duplicate.py` | 重写：规则驱动打分、字段贡献、在途判定 |
| `backend/app/services/customer.py` | 证据文案区分来源；发布前复查同码主档（P0） |
| `backend/app/services/approval.py` | 在途重复专属动作与判断标签 |
| `backend/app/services/import_service.py` | 导入分流对齐主依据口径 |
| `backend/app/repositories/import_repo.py` | 新增 `list_active_for_match()` |
| `backend/app/api/duplication.py` | 查重接口支持名称 + 地址 |

## 四、验证

- 语法编译通过；27 个核心接口全部 200；后端已重启加载新逻辑。
- `_test_duplicate.py`：口径自测（含真实库读取 match_rule 与在途判定）。
- `_test_publish_guard.py`：发布守卫（事务内执行并回滚，不改数据）。

## 五、遗留建议（未改，需你确认）

1. **唯一约束**：是否把 `cmd_customer.credit_code` 改为唯一索引做硬兜底？
   需先确认存量无同码脏数据（当前库只有 1 条，可加）。
2. **提交时快照陈旧**：申请单的 `match_state` 是提交那一刻算的，审批期间存量变化
   不会回写（发布时已重新查重，但列表页仍显示旧值）。若需要，可在审批列表加载时刷新。
3. **`match_rule.auto_merge_flag`**：目前为 N，即不自动合并；与文档「Exact 关联已有」的关系
   可在演示时确认是否需要开启。
