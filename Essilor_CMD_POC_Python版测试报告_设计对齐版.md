# Essilor CMD POC Python 版测试报告（V6.1 设计对齐版）

**测试时间**：2026-09-23
**测试环境**：https://www.essilormdm.dpdns.org/cmd-poc-py/
**测试版本**：Python 重写版
**测试人**：业务测试
**对齐依据**：《Essilor CMD POC 总体设计方案 V6.1 标题调整版》

> 本版在原始测试报告基础上，结合 V6.1 总体设计对以下口径做了对齐修订（原测试观察与结论均保留）：
> ① 查重主依据改为"信用代码 + 经营地址为主、名称仅辅助"（设计 §13 / §09）；
> ② 补充"在途（审批中）重复识别与处置"测试项（设计 §05 / §13）；
> ③ 澄清批量导入模板、动态元数据属"Configuration-first"设计（§11 / §15），为未配置而非代码缺陷。

---

## 一、测试结论总览

| 类别 | 数量 |
|------|------|
| ✅ 历史 BUG 已修复 | 5 项 |
| ❌ 新发现 BUG | 3 项 |
| ⚠️ 配置缺失（非代码BUG） | 4 项 |
| ⏳ 未完成测试项 | 3 项（另补充 1 项设计相关，见第六节） |

**总体评价**：Python 重写版核心流程跑通，上一轮 Vue 版的 5 个关键 BUG 全部修复。但批量导入因模板配置缺失无法完成提交，动态元数据未发布导致 OCR 写回无法关联字段。

---

## 二、历史 BUG 修复验证

| BUG编号 | 描述 | 状态 | 验证结果 |
|---------|------|------|----------|
| BUG-01 | 新建客户跳过审批直接 Active | ✅ 已修复 | 客户提交后进入 Pending，BU Steward 收到任务 AP-20260923-0001，BU 批准后才变 Active |
| BUG-02 | 查重主依据漏判（信用代码 + 经营地址为主，名称仅辅助） | ✅ 已修复 | 第二个客户与 GC-00000002 **信用代码相同、经营地址一致**，命中"精准重复" Match=EXACT；名称作为辅助线索，不再单独以"同名"判定重复（对齐设计 §13 匹配规则 / §09 设计边界） |
| BUG-04 | 提交后弹窗不关闭 | ✅ 已修复 | 提交申请后弹窗自动关闭，回到客户列表 |
| BUG-09 | "全部待办"页签空白 | ✅ 已修复 | 正确显示审批任务列表（AP-20260923-0001/0002） |
| BUG-10 | Auditor 显示待办任务 | ✅ 已修复 | Auditor 为只读角色，工作台无待办按钮，明确标注"仅可查询、查看与导出" |

---

## 三、新发现 BUG

### BUG-PY-01：批量导入"导入模板"下拉无数据，但必填校验拦截提交

**严重程度**：高（阻塞批量导入功能）

**复现步骤**：
1. Business User → 批量导入 → 新建导入任务
2. 选择业务场景 DOOR、BU=High End、来源系统=EXCEL
3. 上传"手册演示_批量导入_四类分流.xlsx"
4. 导入模板下拉显示"无数据"
5. 点提交 → 提示"请选择导入模板"，无法提交

**预期**：导入模板应有可选项（如 Door_Mainstream），或与下载模板联动

**实际**：下拉为空，但表单校验要求必选，导致批量导入完全不可用

**设计对齐（根因）**：导入模板属于"Configuration-first"设计（设计 §15 导入模板），需 Platform Admin 预置并发布后才对业务用户可见；当前环境未配置 Door_Mainstream，故下拉为空。属**配置缺失（非代码缺陷）**，但校验阻断使功能不可用，演示前必须发布模板。

**截图**：new-32-batch-result.png

---

### BUG-PY-02：审批任务行点击不展开右侧详情，需点"进入审批"按钮

**严重程度**：中（UI 交互不直观）

**复现步骤**：
1. Steward BU → 治理与审批 → 全部待办
2. 点击任务行 AP-20260923-0001
3. 右侧仍显示"选择左侧任务查看概要"，详情不展开

**预期**：点击任务行即展开审批详情（与上一版一致）

**实际**：必须额外点击"进入审批"按钮才展开详情。用户容易困惑"为什么点了任务没反应"

**建议**：要么点击行即展开，要么在任务行上明显标注"点击进入审批"

**设计对齐**：设计 §06 审批实例未强制"行点击即展开"，"进入审批"按钮为既有交互方案，故本项属 UX 一致性建议，非设计违规。

---

### BUG-PY-03：OCR 写回后动态字段仍为 0，客户创建信息丢失

**严重程度**：中

**复现步骤**：
1. 新建客户 → OCR 上传营业执照 → 识别成功（5 字段）
2. 点"写回表单" → 提示"OCR结果已写入新建客户申请表（5个字段）"
3. 但动态客户字段区域仍显示"未加载到已发布字段"，0 个字段
4. 提交后审批详情中"提交人"显示"-"，客户名称等信息未在表单中可见

**问题**：OCR 识别了字段并提示写回成功，但因为元数据未发布，字段无法在表单中展示。虽然客户最终创建成功，但业务用户无法在提交前核对/修改 OCR 结果。

**建议**：元数据发布前，OCR 写回应 fallback 到基础字段（名称、信用代码、地址）直接展示在表单上。

**设计对齐（根因）**：动态业务表单依赖"字段与值集"发布（设计 §11 字段与值集 / §12 Master Data Extension），元数据未发布时表单无法渲染字段，属**配置缺失**而非代码缺陷；fallback 到基础字段为 UX 增强建议。

---

## 四、配置缺失项（非代码 BUG，但影响演示）

| 项目 | 当前状态 | 影响 |
|------|----------|------|
| 动态元数据字段 | 0 个，"未加载到已发布字段" | OCR 结果无法回填到表单，业务用户看不到识别内容 |
| DQ 分数 | 显示"—" | 审批详情中 Data Quality 为空 |
| 模型版本 | 显示"…" | 元数据模型未发布 |
| 导入模板列表 | 空 | 批量导入无法提交（BUG-PY-01） |

**设计对齐说明**：动态元数据字段、导入模板、DQ/模型均属"Configuration-first"设计（设计 §11 字段与值集 / §12 DQ / §15 导入模板），需由 Platform Admin 在元数据管理中**发布 / 预置**后才对业务用户生效。因此 OCR 写回字段为空、导入模板下拉无数据、DQ 分数与模型版本为空，均源于**元数据未发布**，属于"未配置"而非代码缺陷；演示前须按下方清单完成配置。

**建议**：客户演示前，务必在 Admin → 元数据管理中发布至少以下字段：
- 客户法定名称、统一社会信用代码、注册地址、省份、城市
- 在「平台管理 › 导入Template」**新建模板 → 配置字段映射 → 发布**（2026-09-23 已支持 UI 建模板；也可用根目录 `_seed_import_template.py` 一键预置）

---

## 五、已验证通过的功能点

### 5.1 Business User 角色
- ✅ 工作台指标正确（客户总数 1、生效中 1、待审批 0→1）
- ✅ 客户管理列表（已生效主档 / 处理中申请 两个页签）
- ✅ 新建客户弹窗正常
- ✅ OCR 上传营业执照 → 识别 5 字段（91%-99% 置信度）
- ✅ OCR 预览图正确显示
- ✅ 提交后自动跳转 Pending 状态

### 5.2 Steward BU 角色
- ✅ 工作台待我处理计数正确（0→1→0→1）
- ✅ 审批任务列表正确显示（任务编号、客户名、场景、BU、Match、SLA、风险）
- ✅ 审批详情展示（One ID、批次号、当前节点、自动检查结果、BU初审判断）
- ✅ 批准确认弹窗正常
- ✅ 批准后待处理计数减少，已处理增加
- ✅ 精准重复客户风险等级 High（正确）

### 5.3 Duplicate Check
- ✅ 新客户 Match=NEW（正确）
- ✅ 同名同信用代码 Match=EXACT（精准重复，主依据为信用代码 + 经营地址一致，名称辅助）
- ✅ 查重回执详细展示命中记录（One ID、客户名、信用代码、BU、记录性质）
- ✅ 提示"批准后将按合并流程关联到 GC-00000002"
- ✅ 生成治理任务 GOV-20260923-0001

> 设计对齐：上述命中 GC-00000002（已发布黄金记录）的 EXACT 场景，BU 初审以「关联已有 / 确认新建 / 退回」处置（设计 §05 治理任务）；若命中对象为**另一条在途（Pending）申请**，则应以「确认重复·撤回本单 / 确认为不同主体·继续新建 / 退回」处置（见第六节补充测试项）。两者已做区分，避免把在途申请误判为"关联已有主档"。

### 5.4 Auditor 角色
- ✅ 只读工作台，无审批/治理按钮
- ✅ 客户数据可见（总数 1、生效中 1、待审批 1）
- ✅ 明确标注"RO 只读审计视图"

### 5.5 GC 角色
- ✅ 工作台正常加载
- ✅ 待处理 0（当前无跨 BU 升级任务）
- ✅ 导航菜单完整（全局治理决策、全局客户主档、层级、批量治理、变更、流程中心、治理审计）

### 5.6 Admin 角色
- ✅ 工作台正常加载
- ✅ 待办事项正确显示（BU初审 1 条）
- ✅ 导航菜单完整（平台管理、集成监控、流程中心、管理员日志）

---

## 六、未完成测试项

| 测试项 | 原因 |
|--------|------|
| 批量导入四类分流（Exact/Suspected/New/Invalid） | BUG-PY-01 阻塞，模板下拉无数据无法提交 |
| 导入明细页签（原 BUG-06） | 同上，无法提交导入任务 |
| GC 跨 BU 合并决策流程 | 重复客户仍在 BU 初审阶段，未升级到 GC |
| 退回补充 / 升级 GC 按钮 | 同上 |
| 变更与停用流程 | 未深入测试 |
| 客户层级（A1/A2/A3） | 未测试 |
| 流程中心 | 未测试 |
| Admin 平台管理 9 张卡片 | 未点入 |
| **在途（审批中）重复识别与处置** | **设计 §05 / §13 要求：申请处于 Pending 时若与另一条在途申请命中，BU 初审应以「确认重复·撤回本单 / 确认为不同主体·继续新建 / 退回」处置，而非「关联已有主档」（关联已有仅用于命中已发布黄金记录）；本轮测试仅覆盖命中已发布主档 GC-00000002 的合并分支，未覆盖在途分支，建议补充** |

---

## 七、测试数据记录

| 客户 | One ID | 信用代码 | BU | 状态 | Match |
|------|--------|----------|-----|------|-------|
| 上海清视眼镜有限公司 | GC-00000002 | 91310106MA1FL2X78K | High End | Active | NEW |
| 上海清视眼镜有限公司（重复） | GC-00000004 | 91310106MA1FL2X78K | High End | Pending（BU初审） | EXACT |

| 审批任务 | 类型 | 状态 |
|----------|------|------|
| AP-20260923-0001 | CUSTOMER_CREATE | 已 BU 批准 |
| AP-20260923-0002 | CUSTOMER_CREATE（重复） | 待 BU 初审 |
| GOV-20260923-0001 | 疑似重复治理 | 待处理 |

---

## 八、给开发的建议优先级

1. **P0 - 配置导入模板（BUG-PY-01 根因）**：在 Admin 预置并发布 Door_Mainstream 导入模板，批量导入方可提交（代码校验无误，根因是未配置）
2. **P0 - 配置元数据**：发布动态字段（至少客户名称、信用代码、地址），否则 OCR 写回无效（属设计 §11/§12 要求的发布动作）
3. **P1 - 修复 BUG-PY-02（UX）**：审批任务行点击应展开详情，或优化交互提示（设计未强制，属一致性建议）
4. **P1 - 配置导入模板联动**：在系统中预置 Door_Mainstream 模板并与下载模板联动
5. **P2 - DQ 分数计算**：当前显示"—"，需确认是否已实现 DQ 引擎（设计 §12 权重为 Proposal）
6. **P2 - 完善 BUG-PY-03**：元数据未发布时 OCR 应 fallback 展示基础字段
7. **P2 - 补充在途重复测试**：验证 Pending 申请命中另一条在途申请时的「撤回本单 / 继续新建 / 退回」处置分支（设计 §05 / §13）

---

## 九、本轮代码修复记录（2026-09-23）

> 说明：原测试报告 3 项 BUG 中，**BUG-PY-03 含一处真实代码缺陷已修复**；BUG-PY-01 / 配置类为部署环境数据未配置，代码侧补了防呆引导。全部改动已在本地后端 + 前端（81）真机回归，未破坏既有流程。

| 编号 | 定性 | 修复内容 | 涉及文件 | 验证 |
|------|------|----------|----------|------|
| BUG-PY-03（提交人） | **真实代码缺陷** | `cmd_approval_task` 建单时从未写入 `applicant_name` → 审批详情「提交人」恒为「-」。现按「前端传参 → 服务入参 → 默认 Business User」写入 `applicant_name / applicant_id`，审计轨迹 `operator_name` 同步由占位值 `applicant` 改为真实提交人 | `backend/app/schemas.py`、`backend/app/services/customer.py` | 真实建单后 `applicant_name='Business User'`；`/cmd/approval/task/{no}/detail` 返回 `submitter='Business User'`；已有 4 条历史任务已回填 |
| BUG-PY-03（OCR 可见性） | 设计对齐增强 | 元数据未发布时动态区渲染 0 字段、OCR 结果无处可见。新增「基础字段（核心主档列）」兜底区（客户法定名称/统一社会信用代码/注册地址），仅当未加载到已发布字段时出现，并与「客户法定名称」必填校验联动；已发布元数据时不渲染，不产生重复字段 | `frontend/.../dialogs/NewCustomerDialog.vue` | 已发布元数据环境回归：弹窗 25 个表单项正常渲染、兜底区不出现、无控制台报错 |
| BUG-PY-01 | 配置缺失 + 防呆 | 模板下拉为空时不再静默：接口异常与「未配置模板」分别给出引导文案（空态 + 提交报错），提示先到「平台管理 › 导入模板」发布模板 | `frontend/.../dialogs/BatchUploadDialog.vue` | 本地模板正常（v1.3 · 9 字段）自动选中；空态/失败文案按分支渲染 |
| BUG-PY-02 | 交互一致性 | 审批任务行点击叠加 `@row-click`（与 `@current-change` 双保险并防重入），确保点行必加载右侧详情；列表头补操作提示「点击任务行查看概要，点右侧「进入审批」办理」；更正过时的 SCSS 注释 | `frontend/.../panels/ApprovalPanel.vue`、`frontend/.../styles/cmd-poc.scss` | Steward BU 真机：点行后右栏显示详情（提交人 Business User），空态未滞留，无控制台报错 |

**结论**：BUG-PY-01 / PY-02 / PY-03 中所有属代码范畴的问题已闭环；剩余「动态元数据字段 / 导入模板 / DQ 分数 / 模型版本」为空仍属**部署环境未配置**（设计 §11/§12/§15 要求由 Platform Admin 发布），本地环境数据齐备（24 个已发布字段 v1.5、2 个导入模板），无需改代码。DQ 引擎（设计 §12「权重为 Proposal，需后续确认」）本轮不实现。

## 十、导入模板可配置化改造记录（2026-09-23 第二轮）

### 10.1 问题复核与定性

| 反馈项 | 复核结论 | 定性 |
|---|---|---|
| 「导入Template 管理」卡片点进去「也是字段管理弹窗」 | 弹窗渲染正确（`dialog: 'template'` → `TemplateDialog`），但其主表就是「源列 / 目标字段 / 必填 / 错误策略」——**视觉上与元数据字段管理高度相似**，且没有任何模板级信息（业务上下文）呈现 | **UX / 语义混淆**（真实缺口） |
| 模板列表 API `/dev-api/cmd/template/list` 返回 404 | 前端源码调用的是 `/cmd/import/template/list`（正确路径，本地实测 200）。404 来自旧构建 / 早期联调脚本使用的短路径 | 旧构建 / 路径差异；已补**兼容别名**消除歧义 |
| 「需要开发在后端预置 Door_Mainstream 模板数据」 | 真实缺口是 **UI 没有"新建模板"能力**：原先只能对已有模板增删字段，全新部署（模板表为空）时管理员在界面上无路可走 | **功能缺口**（已补 UI + 后端接口） |

### 10.2 代码改动（全部位于本项目内）

| 层 | 文件 | 改动 |
|---|---|---|
| 数据访问 | `backend/app/repositories/import_repo.py` | 新增 `insert_template` / `update_template` |
| 业务 | `backend/app/services/import_service.py` | 新增 `save_template`（新建/编辑模板，业务上下文校验；**编码留空按上下文自动生成** `TPL_{场景}_{BU}_{产品线}`，冲突追加 `_2`）、`set_template_status`（发布/停用，**发布前强制校验已有字段映射且含 `legal_name` + `credit_code`**）+ 业务上下文字典常量 |
| 接口 | `backend/app/api/import_api.py` | 新增 `POST /cmd/import/template`、`POST /cmd/import/template/status`；新增只读兼容别名 `GET /cmd/template/list`（等价于真实路径，不进 OpenAPI 文档） |
| 注册 | `backend/app/main.py` | 注册兼容别名路由 |
| 前端类型 | `frontend/src/api/demo/cmdPoc/types.ts` | 新增 `ImportTemplateSaveForm`；`ImportTemplateVO` 补 `id` |
| 前端 API | `frontend/src/api/demo/cmdPoc/index.ts` | 新增 `saveImportTemplate` / `setImportTemplateStatus`；`listImportTemplates` 补 `id` 映射 |
| 前端弹窗 | `frontend/src/views/demo/cmd-poc/components/dialogs/TemplateDialog.vue` | 工具栏重做：**业务上下文摘要条**（场景 / BU / 客户类型 / 产品线 / 来源系统 / 版本 + 模板编码）+ 下载模板 + Admin 的「新建模板 / 编辑模板 / 发布·停用」；映射区加标题「模板字段映射（上传文件表头 → 目标字段）」与字段计数；无模板时引导新建 |
| 前端文案 | `.../dialogs/BatchUploadDialog.vue` | 空态引导改为「新建模板 → 配置字段映射 → 发布」 |

**兼容性**：以上均为**新增能力**，未改动任何既有接口契约或既有调用路径；非 Admin 角色（批量导入页「下载模板」入口）打开同一弹窗仍是**只读**视图（管理类按钮与操作列按 `isAdmin` 隐藏）。

### 10.3 部署辅助：模板预置脚本

`_seed_import_template.py`（项目根目录，**幂等**）——供全新部署 / 清库后一键补齐：

- 预置两个业务上下文模板（均含主键字段映射，直接 Published）：
  `DOOR / Mainstream / Door / Lens / DMS+` → `TPL_DOOR_MAINSTREAM`；
  `DOOR / High End / Door / Frame / Cloud` → `TPL_DOOR_HIGHEND`
- **幂等规则**：模板编码已存在 → **整条跳过**（不新增、不改状态、不补映射），确保对已有环境零影响
- 支持 `--dry-run`（预演）/ `--self-test`（事务内跑新增逻辑后回滚自检）

> 注：全新部署现在也可以**完全在 UI 完成**（平台管理 › 导入Template › 新建模板 → 配置字段映射 → 发布），本脚本仅为可选的一键化便利手段。

### 10.4 验证记录（本地真机）

| 验证项 | 结果 |
|---|---|
| 新建模板（编码留空） | ✅ 自动生成 `TPL_DOOR_HIGHEND_FRAME`，状态 Draft |
| 无字段映射直接发布 | ✅ 被拒：「模板未配置字段映射，无法发布」 |
| 发布 / 停用 | ✅ 状态正确流转；测后已把 `TPL_DOOR_MAINSTREAM` 恢复为 Published |
| 数据还原 | ✅ 自检模板已删除，库内恢复为 2 个模板 / 17 条映射（与改造前一致） |
| 预置脚本 | ✅ 本地运行为 no-op（两模板均跳过）；`--self-test` 写入 8 行映射后回滚，无残留 |
| 接口冒烟 | ✅ 10 个核心接口全 200（含兼容别名 `/cmd/template/list`） |
| 前端类型检查 | ✅ `vue-tsc` 仅 `mock.ts` 的 7 个既有错误，**零新增** |
| 浏览器走查（Admin） | ✅ 卡片 9 张；弹窗标题「导入模板管理」；工具栏含「下载模板 / 编辑模板 / 停用 / 新建模板」；业务上下文摘要 `场景 DOOR · BU Mainstream · 客户类型 Door · 产品线 Lens · 来源系统 DMS+ · 版本 v1.3`；映射 9 行；新建模板端到端成功并自动切换；**console / pageerror = 0** |
| 既有功能回归 | ✅ `_probe_verify_fixes.cjs` 全绿（审批提示 / 点行展开 / 提交人 Business User、新建客户 25 项、批量导入模板提示 v1.3·9 字段），**console error = 0** |

**结论**：图片中反馈的三个问题已全部处理——语义混淆已通过"业务上下文摘要 + 模板级动作"消除；404 已用兼容别名覆盖；"需开发预置数据"这一阻塞已用 **UI 新建模板能力**根治（预置脚本作为可选补充）。既有功能无回归。


